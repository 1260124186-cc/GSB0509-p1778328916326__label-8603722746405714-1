services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: catering-dashboard
    ports:
      - "3000:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    restart: always
